#!/bin/sh

echo ""
echo "🔍 Biome 검사 중..."
echo ""

#* staged 파일만 검사하고, 자동 수정 가능한 건 고쳐서 다시 스테이징
pnpm exec lint-staged

if [ $? -eq 0 ]; then
  echo ""
  echo "✅ 검사 통과!"
  echo ""
else
  echo ""
  echo "❌ 검사 실패 - 커밋 중단"
  echo "   자동 수정이 불가능한 오류입니다. 위 내용을 직접 수정하세요."
  echo ""
  exit 1
fi
